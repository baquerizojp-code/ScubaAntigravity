export default function RootPage() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: '2rem' }}>
      <h1>Next.js boot smoke-test</h1>
      <p>
        Phase A scaffolding only. Real pages arrive in Phase C. The live app
        still runs via Vite on port 8080.
      </p>
    </main>
  );
}
